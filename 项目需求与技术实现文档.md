# 视频去重过原创神器 - 需求与技术实现文档

## 1. 项目概述
本项目是一款基于本地 Python 环境与 FFmpeg 核心引擎构建的“视频去重过原创”工具。主要用于帮助自媒体创作者对搬运或二次剪辑的视频进行深度的底层特征混淆与画面参数微调，从而规避各大短视频平台（如抖音、快手、视频号等）的机器查重算法。
该工具通过本地 Web 端 UI（Gradio 提供）进行操作，完全离线处理，无文件大小限制、无隐私泄露风险。

## 2. 核心需求说明
1. **交互界面需求**：
   - 提供直观的 Web UI 界面，包括四个配置板块：**画面处理、时序与音频、编码与特征、水印与画中画**。
   - 配置项以“分级下拉菜单”呈现（如轻度、中度、重度），抛弃单纯的开/关选项，提升专业度。
   - 提供快捷预设组（轻度去重、中度去重、重度过原创），一键联动修改数十个配置参数。
2. **单文件与预览需求**：
   - 支持上传单个视频文件，进行快速的第一秒画面的“单帧预览”对比（原视频 vs 处理后）。
   - 支持生成完整视频，在网页端提供处理日志与处理后的视频播放。
3. **批量处理需求**：
   - 提供批量处理选项卡，支持一键调用 Mac 原生选择器浏览本地文件夹。
   - 能够自动遍历目录内的常用格式视频（mp4, mov, avi, mkv），静默处理后输出到专属的 `dedup_output` 目录。
4. **核心去重能力需求**：
   - 肉眼不可见的深度去重：元数据清除、文件末尾追加随机 MD5 字节、随机动态 GOP 关键帧结构。
   - 画面级去重：边缘微裁剪、胶片动态噪点、水平垂直微翻转、纹理微量锐化、微量暗角、随机色彩（亮度、对比度、饱和度）微调。
   - 时序/音频去重：抽帧降帧率、时间轴极微变速（改变持续时间）、音量波动、音高（升降调）变化、低频增益模拟录音室音色。

## 3. 技术栈选型
*   **开发语言**：Python 3
*   **前端交互界面**：`Gradio` (轻量级的 Python 机器学习/数据展示 UI 库)
*   **核心引擎**：`FFmpeg` (跨平台音视频处理库)
*   **Python FFmpeg 绑定**：`ffmpeg-python`
*   **原生弹窗调用**：Mac `AppleScript` (通过 `subprocess` 调用 `osascript` 提供原生的本地文件夹选择体验)

## 4. 技术架构与模块划分
项目包含两个核心 Python 文件，职责分离清晰：

### 4.1. 交互与状态层 (`app.py`)
负责整个应用的前端渲染、路由以及事件绑定：
*   **界面渲染**：使用 `gr.Blocks`, `gr.TabItem`, `gr.Accordion`, `gr.Dropdown` 构建了复杂的表单树。
*   **预设联动机制**：`update_presets()` 函数负责接收预设指令，并返回 24 个参数的具体状态，通过 Gradio 的事件系统触发界面所有下拉框的批量更新。
*   **Mac 原生文件夹弹窗**：为了绕开 Gradio 的安全限制，使用 `select_local_folder()` 调用 `osascript -e 'POSIX path of (choose folder)'` 来唤出原生的“选择文件夹”对话框。
*   **网络代理冲突解决**：通过注入 `os.environ["no_proxy"] = "localhost, 127.0.0.1, ::1"`，避免 Gradio 本地端口被系统梯子/加速软件拦截导致 `502 Bad Gateway` 报错。

### 4.2. 核心编解码与滤镜层 (`core_ffmpeg.py`)
所有的视频计算与封装都在此层完成。将几十个下拉菜单的值解析翻译为复杂的 `ffmpeg filter graph`。

#### (1) 画面处理链 (`Video Filter Graph`)
*   `fps`：指定固定帧率以实现轻微抽帧处理。
*   `setpts`：配合 `1.0/speed * PTS` 改变视频的播放总时长，破坏时长特征。
*   `crop` & `scale`：按照百分比裁剪边缘（1%~3%），并将其缩放回原始分辨率，混淆画面四角的边界特征。
*   `hflip` & `vflip`：实现水平/垂直翻转。
*   `unsharp`：控制不同级别的 `luma_msize` 和 `luma_amount`，在频域进行锐化处理，改变画面微弱纹理。
*   `noise`：增加时域随机噪点（`c0f='t+u'`），确保每一帧都在随机抖动，破坏视频指纹 MD5。
*   `eq`：控制 `brightness`, `contrast`, `saturation` 对色彩空间做全局偏移映射。
*   `vignette`：模拟暗角效果叠加一层黑边遮罩。

#### (2) 时序与音频处理链 (`Audio Filter Graph`)
*   `atempo`：补偿由于画面变速导致的时间差，让音频时长同步伸缩。
*   `volume`：统一增加或随机波动音量。
*   `asetrate`：调整采样率从而实现真实的升调/降调处理，这是逃过音频查重非常有效的方法。
*   `afftdn` / `equalizer`：前者做降噪处理改变频谱，后者进行低频放大（特定频率+20dB），改变音频频谱能量分布特征。

#### (3) 编码与元数据处理 (`Encoding Args`)
*   使用 `libx264` 编码器和 `crf=18` 维持高画质。
*   `map_metadata='-1'` 强行抹除所有相机、GPS、剪辑软件遗留的原始头信息。
*   动态随机配置 GOP (关键帧间距 `g`) 和 B 帧数量 (`bf`)，甚至随机挑选 H264 Profile（`main` 或 `high`），在二进制层面彻底改变 H264 的打包规律。
*   通过 `maxrate` 和 `bufsize` 激活 VBR 动态码率，改变文件体积分布。

#### (4) 纯物理哈希干扰 (`MD5 Appending`)
在视频导出完成后，以二进制追加写模式 (`ab`) 向文件的最尾部写入一串长度随机（10-50 字节）的 `os.urandom()` 数据。对于视频播放器而言，文件末尾的乱码不会影响正常播放，但对于平台的文件级 MD5 哈希校验，这是一个全新的文件。

## 5. 部署与运行
1. 环境要求：Mac 系统，已安装 Python 3 和全局 `ffmpeg` 命令工具。
2. 依赖安装：
```bash
pip install gradio ffmpeg-python
```
3. 启动指令：
```bash
python3 app.py
```
控制台启动后会自动打开 `http://127.0.0.1:7860` 界面。
